"""Smoke test — verifies pytest infrastructure collects and runs at least one test."""

from __future__ import annotations


def test_smoke() -> None:
    """Verify pytest infrastructure is operational."""
    assert True
