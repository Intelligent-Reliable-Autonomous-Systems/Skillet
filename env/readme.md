# Considerations for environment
The environment should define the robot, have the jacobians, forward/inverse kinematics, etc
The EE policies (low level policies) should have to pull from the environment to get the required
jacobians 