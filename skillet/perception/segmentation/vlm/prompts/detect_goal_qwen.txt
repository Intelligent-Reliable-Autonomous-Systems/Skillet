Based on the task instruction: "{task_instruction}", translate this natural language instruction into some conjunction of formal predicates:

AVAILABLE PREDICATES:
- on(movable, surface): Object A is placed on top of object B

For instance - for the task "place the red block on the purple block" the goal should be:
    {{"goal": {{"predicate": "on", "args": ["red block", "purple block"]}}}}


Return a JSON list like: '[{{"goal": {{"predicate": "on", "args": ["yellow block", "red block"]}}}}, ...] '


