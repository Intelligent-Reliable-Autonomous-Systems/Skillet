Perform two tasks on this image based on the task instruction: "{task_instruction}".

TASK 1 - OBJECT DETECTION:
Detect and return bounding boxes for objects in the image.
- DO NOT include the robot, robot gripper, table surface, red button, or any april tags
- DO NOT include objects or surfaces irrelevant to the task or too far away to matter (e.g. walls, things on the wall that are far away, things on the floor below the table, people who might be in the scene, etc.)
- Limit to 25 objects
- If an object appears multiple times, name them by unique characteristics (color, size, position, etc.). If they seem the same, then just use numbers (e.g. 'soda_can1' and 'soda_can2', ... for identical-looking soda cans)
- An object **cannot have the same name** as another under any circumstance.
- Format: normalized coordinates 0-1000 as integers

Be very careful to identify objects and name them in a way that's relevant to the task. If the task involves picking up a red apple, make sure that 'red' appears in the name of the apple.


TASK 2 - TASK SCENE TRANSLATION:
Describe the current image using formal predicates so that the natural language instruction will be solvable:

AVAILABLE PREDICATES:
- on(movable, surface): Object A is placed on top of object B

It is very important that the scene is exact: use your visual recognition, common-sense and reasoning abilities to make sure the scene expression is perfectly accurate. Make sure that it makes sense that the goal can be reachable from the scene predicates created.

For instance - for the task "move the purple block onto the red block" when there is a red block on top of a blue block, and a purple block on the table, the scene should be: 

"predicates": [
        {{"name": "on", "args": ["red_block", "blue_block"]}},
        {{"name": "on", "args": ["blue_block", "table"]}},
        {{"name": "on", "args": ["purple_block", "table"]}},
]

Return a single JSON object with this structure (no code fencing):
{{
    "bboxes": [
        {{"box_2d": [ymin, xmin, ymax, xmax], "label": "object name"}},
        ...
    ],
    "scene_predicates": [
        {{"name": "predicate_name", "args": ["object1", "object2"]}},
        ...
    ]
}}


Use the object labels you detect in Task 1 when creating predicates in Task 2. You may use the Table as well for the predicates in Task 2, but it should not appear in Task 1.
Only reference objects that you actually detected in the image with the exception of the Table. 